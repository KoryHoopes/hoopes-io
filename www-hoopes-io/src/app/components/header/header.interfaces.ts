export interface Site {
  title: string;
  email: any;
  baseUrl: string;
  mobileMenu: {
    icon: any;
    show: boolean;
  };
}

export interface SocialMedia {
  name: string;
  icon: any;
  url: string;
}
